VMware : Outil pour créer des machines virtuelles. Simuler une machine avec ou sans OS via un disque virtuel sur son ordinateur.
Fichier .vmdk : Virtual Machine Disk -> Disque émulant une machine

Se loguer avec login + password.

Arrêter le serveur : halt

CTRL + ALT : revenir sur le PC

Récupérer l'adresse IP (10.213.93.75) puis la mettre dans putty.exe
Réduire VMware et laisser la machine tournée.

Putty.exe -> c'est un client SSH, émulateur de terminal. Logiciel libre et en version portable.
SSH (Secure shell) : Pouvoir travailler sur un serveur de façon sécurisé (chyffré) via un terminal. 
Protocole de transfert sécurisé.

Selection avec clic gauche : copie le texte dans le presse papier
Clic droit = coller

Utiliser du http : curl -Xrequete

Utilisation de WinSCP (Service SCP -> fichier over ssh et SFTP -> SCP + prendre la main sur la gestion des controles de fichiers)
C'est un client SFTP open source. Le but étant de sécuriser le transfert de fichiers entre un ordinateur local et distant.

Explications sur le tar.gz
.tar est un type de fichier d'archive comprenant une liste de fichiers non compressés. 
.gz est un format compressé.

Il est important de faire la distinction entre fichier d'archive et de compression car ce n'est pas la même chose.

Commande linux pour extraire un tar.gz : tar xzf nomDuFichier

En réalité, les formats visibles sur Windows ne sont pas forcémment le réel type de fichier en question.

Quelques infos en réseau :

Socket = IP + port

Port = service / processus (route pour atteindre le logiciel qui tourne)

Le serveur DHCP fournit les adresses IP aux machines sur le réseau avec une table sous forme : MAC / IP

Le FTP est un protocole vieux qui ne sécurise pas les données transmises et nécessite l'utilisation de deux ports (20 et 21)
Le protocole SSH (SCP et SFTP) est à privilégier car il utilise un seul port (22) et sécurise les données envoyées et reçues.

Commandes Linux :

-history : historique de commandes
-vi / vim : éditeur de texte
-rm : supprime un fichier
-grep : filtre avec le nom donné en argument
-ifconfig : infos réseau

| = entrée / sortie pour plusieurs commandes

Utiliser Wireshark (anciennement Ethereal) pour voir ce qui se passe sur la carte réseau

Commandes sous redhat :

-yum install : Installer un logiciel
-yum search nomLogiciel : Cherche un logiciel

Utiliser une commande propre au dossier actuel : ./nomDeLaCommande

Afficher l'adresse IP : ip addr show